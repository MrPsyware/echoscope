name: echoscope-photos
services:
  photos:
    build: .
    ports:
      - "0.0.0.0:${PHOTO_PORT:-8086}:8086"
    restart: unless-stopped
    read_only: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 128m
    pids_limit: 64
